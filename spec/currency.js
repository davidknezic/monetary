describe("Currency management", function () {
  it("should add a new currency", function () {
  });

  it("should add and return a new currency", function () {
  });

  it("should remove an existing currency", function () {
  });

  it("should silently replace when setting a duplicate currency", function () {
  });

  it("should throw an error when setting an invalid currency", function () {
  });

  it("should list all available currencies", function () {
  });
});
