describe("Locale management", function () {
  it("should add a new locale", function () {
  });

  it("should remove an existing locale", function () {
  });

  it("should add and return a new locale", function () {
  });

  it("should return the currently active locale", function () {
  });

  it("should return a list of all locales", function () {
  });

  it("should intervene not to delete the last locale", function () {
  });
});
